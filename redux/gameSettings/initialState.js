export const initialState = {
  timer: 'on',
  solver: 'basic',
  region_highlighting: true,
  row_column_highlighting: true,
  hide_duplicates: false,
  prevent_mistakes: false,
  auto_remove_notes: false,
}
